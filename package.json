{
  "name": "secure-file-transfer-cloudflare",
  "private": true,
  "scripts": {
    "postinstall": "npm ci --prefix backend && npm ci --prefix frontend && npm run build --prefix frontend",
    "build": "npm run build --prefix frontend",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.107.1"
  }
}
